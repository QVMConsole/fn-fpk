#!/bin/bash
APP_BIN="${TRIM_APPDEST}/server/qvmconsole-manager"
chmod 0755 "$APP_BIN"
"$APP_BIN" storage-mount-compat
if command -v systemctl >/dev/null 2>&1; then
  systemctl daemon-reload
fi
exit 0
